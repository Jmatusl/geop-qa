import { PrismaClient } from "@prisma/client";

/**
 * Seed mock para el módulo Requerimientos - Actividades.
 * DESHABILITADO: No se cargan datos mock para este módulo.
 * Los requerimientos se crean manualmente desde la interfaz.
 */
export async function seedActividadesMock(prisma: PrismaClient) {
  console.log("  📋 Mock Data — Requerimientos de Actividades [OMITIDO]");
  // No se cargan datos mock para actividades
  return;
}
